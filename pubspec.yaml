name: qr_vpa_scanner_trial
description: A trial project for QR VPA scanner app

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  qr_code_scanner: ^0.5.2
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  path_provider: 0.4.1
  webview_flutter: ^2.0.10

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
